# uninstall the current bcrypt modules
npm uninstall bcrypt @types/bcrypt

# install the bcrypt modules for the machine
npm install bcrypt

# install the bcrypt typescript modules for the machine
npm install --save-dev @types/bcrypt